# AegisQA PRD 验收覆盖矩阵

本文按 `AI_Automation_Eval_Workflow_PRD_final(1).md` 的功能编号记录当前实现状态。

## 当前验证命令

```powershell
python -m pytest -q
python -m pytest tests\test_sqlite_store_adapter.py -q
python -m aegisqa.examples.run_mvp_demo
cd frontend
npm run typecheck
npm test
npm run build
npm run e2e
Headless Chrome CDP 打开 http://127.0.0.1:5173 并点击核心页面按钮
```

最近一次验证结果：

- 本轮新增报告中心参数风险诊断动作闭环验证：`cd frontend && npm test -- src/test/ReportsPage.test.tsx -t "参数风险诊断"` 先 RED 后 GREEN，确认后端诊断返回 `plan_workflow_parameter_changes` 时，前端显示“规划 Workflow 参数变更”，并通过修复任务 API 生成参数 diff 与回滚计划，不再展示裸动作名或 unsupported 错误；`cd frontend && npm test -- src/test/ReportsPage.test.tsx` 和 `cd frontend && npm run typecheck` 均通过。
- 本轮新增 Skill package 生命周期元数据真实操作者归因验证：`python -m pytest tests\test_skill_package_security.py::test_skill_package_records_contract_and_approval_metadata -q` 先 RED 后 GREEN，确认上传插件包审批后的 `approved_by`、`approved_by_role` 和 `approval_history` 会记录请求 actor/role；`python -m pytest tests\test_skill_package_security.py tests\test_agent_skill_package_upload.py tests\test_agent_skill_runtime_matrix.py -q`、`python -m pytest tests\test_access_control_hardening.py -q`、`python -m pytest tests\test_skill_market_versions.py -q`、`python -m pytest -q --durations=20` 均通过。
- 本轮新增 Baseline 生产切换审计 role 归因验证：`python -m pytest tests\test_task_flow_optimization.py::test_prompt_skill_candidate_retest_requires_published_draft_and_returns_three_way_metrics -q` 先 RED 后 GREEN，确认候选资产晋升后的 Baseline apply、Baseline 变更提醒 ack 和 Baseline rollback 成功审计会记录请求 actor/role，并在 `detail.role` 中保留角色上下文；`python -m pytest tests\test_task_flow_optimization.py -q -k "baseline or promotion or candidate"`、`python -m pytest tests\test_access_control_hardening.py -q`、`python -m pytest tests\test_ci_baseline_production_flow.py -q`、`python -m pytest -q --durations=20` 均通过。
- 本轮新增 Repair Task 派生资产审计归因验证：`python -m pytest tests\test_task_flow_optimization.py::test_repair_task_retest_action_creates_attempt_and_compares_result tests\test_task_flow_optimization.py::test_repair_task_version_diff_actions_materialize_candidate_and_workflow_draft -q` 先 RED 后 GREEN，确认 `retest_and_compare` 创建的新 Attempt 和 `create_workflow_draft_from_version_diff` 创建的 Workflow 草稿会记录请求 actor/role；`python -m pytest tests\test_task_flow_optimization.py -q -k "repair_task or prompt_skill_candidate"`、`python -m pytest tests\test_access_control_hardening.py -q`、`python -m pytest tests\test_productization_api.py -q -k "candidate or annotation or ci_gate"`、`python -m pytest -q --durations=20` 均通过。
- 本轮新增 Agent Skill 本机导入权限与审计归因验证：`python -m pytest tests\test_access_control_hardening.py::test_agent_skill_import_requires_permission_and_records_actor_role -q` 先 RED 后 GREEN，确认 `POST /agent-skills/import` 要求 `skill:register`，Viewer 被结构化拒绝，Skill Developer 成功审计记录真实 actor/role；`python -m pytest tests\test_access_control_hardening.py -q`、`python -m pytest tests\test_agent_skill_runtime.py tests\test_agent_skill_runtime_matrix.py -q`、`python -m pytest tests\test_agent_skill_package_upload.py tests\test_skill_package_security.py -q`、`python -m pytest -q --durations=20` 均通过。
- 单元/API/扩展测试：`python -m pytest -q --durations=20` 已通过，覆盖后端全量测试；仍有 Windows `.pytest_cache` 创建警告，不影响结果。本轮新增并验证 Task Preflight / 执行参数模板权限与审计归因，确保执行模板创建和 Task Preflight 要求 `run:create`，Viewer 被结构化拒绝，Evaluator 成功审计记录真实 actor/role，任务创建前证据链不再固定记录为 `api`；继续保留 Judge 治理权限与审计归因，确保 Judge Profile 创建、一次性 Judge 审计和 Profile 审计要求 `judge:audit`，Viewer 被结构化拒绝，Reviewer 成功审计记录真实 actor/role，Judge 治理动作不再固定记录为 `api`；继续保留 Dataset 治理权限与审计归因，确保 Source 物化和字段类型修正继续要求 `dataset:create`，Viewer 被结构化拒绝，Source 物化 forbidden 审计使用具体 `dataset.source_materialize`，字段修正 forbidden 审计使用 `dataset.field_type.correct`，Evaluator 成功审计记录真实 actor/role；继续保留 Red Team / Experiment / CI Gate 权限与审计归因，确保 Red Team 扫描要求 `redteam:scan`，Experiment 快照创建要求 `experiment:create`，CI Gate 创建和评估要求 `ci_gate:manage`，Viewer 被结构化拒绝，Evaluator 成功审计记录真实 actor/role，安全扫描、实验快照和发布门禁评估不再固定记录为 `api`；继续保留 Candidate Assets 权限与审计归因，确保单条审核、批量审核、批量指派、批量归档、逾期升级、生成 Workflow 草稿、候选复跑、创建晋升审批和晋升审批结论要求 `candidate:govern`，Viewer 被结构化拒绝，Evaluator/Reviewer 成功审计记录真实 actor/role，候选复跑和晋升发布资产不再固定记录为 `api`；继续保留 Annotation Queue 权限与审计归因，确保生成、自动分派、指派、单条审核和批量审核要求 `annotation:review`，Viewer 被结构化拒绝，成功审计记录真实 actor/role，候选资产 reviewer 使用真实 actor；继续保留 Repair Task 权限与审计归因，确保生成、领取、指派、完成、重开和动作入口要求 `badcase:correct`，Viewer 被结构化拒绝，成功审计记录真实 actor/role；继续保留 Workflow 资产权限与审计归因，确保草稿创建/更新/删除、复制、归档和发布写操作要求 `workflow:publish`，Viewer 被结构化拒绝，成功审计记录真实 actor/role；继续保留 Task 执行控制权限与审计归因，确保暂停、恢复、取消、重试失败项要求 `run:control`，新建 Attempt 要求 `run:create`，Viewer 被结构化拒绝，成功审计记录真实 actor/role；继续保留模型网关治理权限与审计归因，确保 `PUT /model-gateway/config`、模型连接别名增删改和 `/model-gateway/test` 都要求 `model:configure`，成功/拒绝审计记录真实请求 `actor` 与 `role`，且审计不包含明文 API Key；继续保留 Task 原始结果导出成功审计归因，确保 `task.results.export` 记录真实请求 `actor` 与 `role`；继续保留 Task 结果 CSV 与 Task 报告 CSV 的公式注入防护，避免 `= + - @` 前缀被 Excel/WPS 当公式执行；继续保留 legacy Run HTML 报告导出转义，避免用户样本、Badcase payload 或模型输出里的脚本片段进入 HTML；同时保留 `JsonStore.list_json()` 文档级读锁，避免 Windows 并发读取 Skill 包列表时阻塞原子写入。
- SQLite/MySQL 仓储：`python -m pytest tests\test_sqlite_store_adapter.py -q` 已通过，覆盖 SQLiteStore JSON/JSONL 读写、legacy JSON 回退、FastAPI Task 主链路、列表接口和并发审计流 JSONL append，不会在多人同时写审计事件时重复分配 `row_index`；`python -m pytest tests\test_mysql_store_adapter.py -q` 已通过 fake MySQL 契约测试，覆盖核心表镜像、FastAPI Task 主链路、运行态状态和 MySQL JSONL stream named lock 并发 append。
- 端到端 Demo：最新 Dataset `rag_qa_1000:v13`、Run `run-e46e560e1885`，1000 条 JSONL 样本状态 `completed`，队列消息字段仅 `item_id`，报告 `pass_rate=0.8`、`error_rate=0.0`、Badcase 200 条，Judge 审计输出 Accuracy / Precision / Recall / F1 / Cohen's Kappa / Confusion Matrix。
- 前端：`npm run typecheck`、`npm test`、`npm run build` 已通过；最近目标测试 `cd frontend && npm test -- src/test/ReportsPage.test.tsx -t "参数风险诊断"` 已 RED/GREEN 通过，确认报告中心会把后端诊断动作 `plan_workflow_parameter_changes` 显示为“规划 Workflow 参数变更”并串联修复任务参数 diff 计划；模型网关目标测试 `cd frontend && npm test -- src/test/App.test.tsx -t "历史误填的模型名 Provider|模型网关配置|模型连接别名"` 已 RED/GREEN 通过，确认模型 Provider 为协议下拉且只暴露 `mock/openai_compatible` 用户语义选项，具体模型名进入默认模型下拉；默认模型搜索只过滤候选，不再隐式创建自定义模型；历史误把 `deepseek-v4-flash` 写进 Provider 的配置会在前端迁移为 `openai_compatible` 并保留原模型名作为默认模型候选；`npm test` 当前为 12 个测试文件、133 个测试，覆盖交互/API client/图模型/Workflow 深度交互/Workflow 市场删除复制筛选/草稿优先发布/schema 驱动输入绑定与输出写入/映射预览数据集字段预览/Skill Palette 搜索/Skill 合约测试结构化结果/Agent Skill zip 包上传入口/任务创建向导/任务级 Skill 参数覆盖/schema-driven 参数选择/表达式路径候选/非法 JSON 覆盖值前端拦截/Preflight 创建门禁/Workflow 发布失败错误建议/关键参数签名新鲜度/Preflight 证据展示/执行参数模板/模板 ID 创建提交/Run Attempts/Dataset Lineage/Trace Tree/Experiment/CI Gate/Annotation Queue/候选资产中心、报告中心、修复任务工作台、Judge 偏差趋势、治理边界和任务创建向导生命周期；`App.test.tsx` 已抽出共享 harness，并把修复任务工作台、报告中心与 Workflow 深度交互迁移到独立测试文件；全量 Playwright `npm run e2e` 已通过，14 passed。
- 浏览器交互：Headless Chrome CDP 验证概览、Skill 市场、Workflow 市场、Workflow 画布、任务列表、任务报告均能打开并展示关键入口。
- Playwright E2E：`npm run e2e` 14 passed，真实覆盖上传 JSONL 数据集、上传 zip Skill 插件包、运行合约测试、治理启用 Skill、发布 Workflow、创建并执行 Task、执行中心搜索任务、报告中心远程搜索任务报告、导出报告、Badcase 加入 Golden；同时覆盖 CI Gate 创建配置和阻断评估、Annotation Queue 领取/审核/回流 Golden、批量审核和候选资产摘要，以及 Workflow 画布新增 Source/Skill/Join/Output/Aggregator、聚合策略、创建连线、删除节点、删除下游连线、删除后重连、节点工具栏、键盘删除、撤销/重做、schema 驱动字段路径编辑和保存回放、保存草稿留在画布后返回市场回放、试运行回填、校验、草稿发布。
- 文档教程：README、`docs/AGENT_SKILL_PACKAGE_GUIDE.md`、`docs/SKILL_EXAMPLES_INDEX.md` 与 `docs/tutorials/` 已统一串联，覆盖“5 分钟跑通 ASR/QA 评测”“写一个脚本型 Skill”“接入真实模型”三条新机器上手路径；`tests/test_documentation_tutorials.py` 固定教程入口、示例包索引和模型网关关键参数。
- 产品化增强：Experiment 快照、Assertion DSL、CI Gate、Annotation Queue、Trace Tree、Task Preflight、Repair Task 工作台、动作闭环、复跑对比、上下文修复建议与二级修复任务已有后端 API 测试；首页已展示真实 Dashboard 和产品化增强入口。

## P0 功能覆盖

| 编号 | 需求 | 当前状态 | 证据 |
|---|---|---|---|
| FR-SK-01 | 扫描/注册 Skill manifest 并展示市场 | 已实现 | `aegisqa/skills/registry.py`，`GET /skills`，React `Skill 市场` 页面，测试 `test_skill_registry_loads_example_manifests_and_contracts` |
| FR-SK-02 | config_schema 生成参数表单 | 已实现基础 | Skill manifest 暴露 `config_schema`；React Skill 详情抽屉展示 schema；Workflow Inspector 会按当前 Skill 的 `config_schema` 渲染参数表单，支持 string/number/integer/boolean/enum/object/array 基础输入，并把配置写入 Workflow 草稿 graph；任务创建向导也会按所选 Workflow Step 反查 Skill `config_schema`，将任务级覆盖参数名升级为下拉选择并自动推断值类型，表达式路径会从当前 Dataset Version 的 `field_paths` 或 `field_schema` 生成候选，JSON 覆盖值会在运行 Preflight 前做前端合法性校验，减少手写 `skill_overrides`、`row.xxx` 路径和坏 JSON 的出错率；Workflow Graph 发布会按 `config_schema.required` 和固定值类型阻断缺失或错误参数，Task Preflight 会结合 Dataset 预览样本、Workflow config 和任务级 `skill_overrides` 校验最终参数，表达式参数会返回出错 `row_index`；Workflow Console 会把 `CONFIG_REQUIRED_MISSING`、`CONFIG_VALUE_INVALID`、`CONFIG_EXPRESSION_PATH_MISSING`、`CONFIG_SECRET_REF_EMPTY` 等参数错误转成中文修复建议，指向参数表单、任务级覆盖、Dataset 预览样本和 Secret 引用；动态复杂联动表单仍可增强 |
| FR-SK-03 | input/output schema 强校验 | 已实现 | `aegisqa/core/mapper.py`，`BaseSkill.execute`，TypeMismatch 测试 |
| FR-SK-04 | Skill 版本、作者、依赖、场景、标签 | 已实现 | `SkillManifest` |
| FR-SK-05 | Skill 合约测试入口 | 已实现 | `BaseSkill.contract_test()`，`POST /skills/{skill_id}/contract-test`，React Skill 详情按钮测试；Skill 市场会解释合约测试使用 manifest 的 `example_input` 和 `example_config` 执行一次 Skill，并结构化展示测试输入、测试配置、输出结果、耗时、错误码、错误信息和修复建议；插件包审批前也必须通过合约测试，插件包记录会保存 `last_contract_ok`、`last_contract_result`、`last_contract_at` |
| FR-SK-08 | Skill 插件包上传与审批门禁 | 已实现基础 | `POST /skills/packages/upload`、`GET /skills/packages`、`aegisqa/skills/packages.py`；Skill 市场主入口改为上传 Agent Skill zip 包，支持 `SKILL.md + skill.yaml/json + scripts/run.py` 的 `runtime.mode=script` 脚本型 Skill，也支持 `SKILL.md + skill.yaml/json + references/` 的 `runtime.mode=instruction_model` 说明型 Skill；旧版 `skill.yaml|skill.json + handler.py` 继续兼容；纯参数或脚本型 Skill 缺少 manifest 会拒绝，因为平台无法生成输入、输出和运行参数表单；上传后默认 `pending_review`，非法 zip 路径会拒绝，合约测试默认 60 秒超时并返回 `SKILL_CONTRACT_TIMEOUT`，stdout 超过安全上限返回 `SKILL_PACKAGE_OUTPUT_TOO_LARGE`，运行时 stdout/stderr 会截断并脱敏本地绝对路径，审批记录保存审批人、审批时间和审批备注；本机 Agent Skill 导入接口 `POST /agent-skills/import` 也要求 `skill:register`，Viewer 会被结构化拒绝，Skill Developer 成功审计记录真实 actor/role；测试 `test_agent_skill_package_upload.py`、`test_skill_package_security.py`、`test_agent_skill_runtime.py`、`test_agent_skill_runtime_matrix.py` 和 `test_access_control_hardening.py`；Playwright 主链路覆盖真实 zip 上传、合约测试和治理启用 |
| FR-WF-01 | 创建、编辑、复制、发布、归档 Workflow | 已实现基础 | `WorkflowService.publish/copy_workflow/archive`，`POST /workflow-graphs/publish`，`POST/PUT/DELETE /workflow-drafts`，`GET /workflow-drafts?status=deleted`，`GET /workflow-drafts/{draft_id}` 单草稿深链加载，React `Workflow 市场` + `Workflow 画布`；市场页支持草稿复制/删除、已发布版本复制为草稿/归档和状态筛选；画布有草稿 ID 时优先调用 `POST /workflow-drafts/{draft_id}/publish`，没有草稿时先保存再发布；市场进入画布会预写单草稿缓存，画布加载完成前显示加载态，避免后台刷新覆盖用户编辑；草稿创建/更新/删除、复制、归档、草稿发布、Graph 发布和线性发布均要求 `workflow:publish`，成功/拒绝审计记录 actor/role |
| FR-WF-02 | 线性步骤列表与图形化编排 | 已实现 | `WorkflowDraft` / `WorkflowStep`，`WorkflowGraph` 保留画布快照；前端图模型已独立测试，Inspector 支持节点工具栏、键盘删除、下游连线可视化、选择目标创建连线、删除连线、删除后重连和 Aggregator 聚合策略，Playwright 覆盖 Source/Skill/Join/Output/Aggregator 新增、创建连线、删除节点、删除下游连线、删除后重连、撤销、重做、保存草稿回放、校验、发布 |
| FR-WF-03 | 字段映射与强类型校验 | 已实现 | `resolve_input_mapping`，失败不调用 Skill；Workflow 图校验会读取 Skill `input_schema.required`，在校验/发布阶段阻断必填输入映射缺失并返回 `REQUIRED_INPUT_MAPPING_MISSING`；线性兼容发布入口 `/workflows/publish` 也会校验 Workflow Step 合约，阻断缺少必填映射、空输入路径和空输出路径；Task Preflight 新增 `workflow_schema_mapping` 检查项，可拦截历史坏 Workflow；Workflow Graph 发布和 Task Preflight 也会校验 Skill 参数合约，避免参数缺失或类型错误拖到执行期；React Workflow Inspector 已把“字段映射”拆成 schema 驱动的“输入绑定 / 输出写入 / 运行参数”，输入输出左侧字段由 Skill `input_schema` / `output_schema` 生成，高级 JSON 模式仅作为兼容折叠项；选择映射预览数据集后会展示字段名、类型、示例值和 `row.xxx` 路径，并让输入绑定候选可选；Workflow Console 对输入绑定、未知 Skill 和参数门禁错误均展示可操作修复建议，Playwright 覆盖 `row.prompt_text` 路径保存草稿并从 Workflow 市场重新打开后回放 |
| FR-WF-04 | 试运行 | 已实现基础 | `WorkflowRunner.dry_run` 支持 1-10 条样本，`POST /workflow-graphs/dry-run`，React Console 已接入按钮反馈，Playwright 覆盖选择数据集后试运行回填结果 |
| FR-DS-01 | CSV/JSONL 上传与流式解析 | 已实现 | `DatasetService.upload_dataset`，API `/datasets/upload` 与 `/datasets/from-path`，React 上传弹窗与禁用反馈已测试；空文件、坏 JSONL 行号、空 CSV 均有结构化错误 |
| FR-DS-02 | 数据集版本化 | 已实现 | 同名上传递增版本 |
| FR-DS-03 | 字段类型识别与人工修正 | 已实现基础 | 自动识别 + `DatasetService.correct_field_type` + API 字段修正；字段修正要求 `dataset:create`，Viewer 会被结构化拒绝，成功/拒绝审计记录 actor/role |
| FR-DS-04 | Source Skill 机制 | 已实现基础 | `source.csv@0.1.0`、`source.jsonl@0.1.0` manifest，`POST /datasets/source-materialize`；Source 物化要求 `dataset:create`，Viewer 会被结构化拒绝，成功/拒绝审计记录 actor/role |
| FR-DS-05 | Golden Dataset 与人工标签 | 已实现基础 | `golden`、`label_field`、Judge demo |
| FR-DS-06 | rows 按需读取 | 已实现 | `iter_rows` / `iter_row_chunks`，队列消息仅 `item_id` |
| FR-DS-09 | Dataset Lineage | 已实现基础 | `GET /datasets/{dataset_id}/versions/{version}/lineage` 返回来源类型、来源参数、字段路径、预览和下游 Task；React 数据集页提供“查看 Lineage”抽屉 |
| FR-EX-01 | 分片创建 Run Items 与轻量队列 | 已实现 | `WorkflowRunner.create_run`，1000 样本测试；Task API 将 Dataset/Workflow/Run 绑定为一次业务任务；Task 创建保存评测目标、质量门槛和 Preflight 快照；产品语义中 Task 是用户主对象，Run 是底层执行 Attempt；`GET /runs` 无分页参数时保持 legacy 完整 `RunRecord[]`，带 `page/page_size` 时返回轻量 Attempt 摘要分页，并支持 `status/dataset_id/workflow_version_id` 筛选，列表项不返回 `items` 和 `queue_messages` 大字段 |
| FR-EX-02 | 并发控制与外部 API 限速 | 已实现基础 | `InMemoryRateLimiter` 记录等待与限速次数；Redis/Celery 适配待生产化 |
| FR-EX-03 | 失败重试 | 已实现基础 | `retry_failed_items` |
| FR-EX-04 | 断点续跑 | 已实现基础 | 成功 item 不覆盖，失败 item 可重跑 |
| FR-EX-05 | Step 轨迹 | 已实现 | 输入/输出摘要、耗时、异常、指标、cache_hit、限速等待；`GET /runs/{run_id}/trace` 无分页参数保持 legacy 完整响应，带 `page/page_size` 时返回分页后的 items 和 pagination，避免大 Run 一次性序列化全部样本明细 |
| FR-RP-01 | Run 指标聚合 | 已实现 | `aggregate_run_report` 单次遍历 Run Item，聚合完成/失败、通过率、Badcase、latency、item metrics 和模型 usage/cost，避免报告接口对同一 Run 明细重复扫描 |
| FR-RP-02 | Skill 输出指标入库 | 已实现 | Item metrics 与 report 聚合 |
| FR-RP-03 | Badcase 明细筛选 | 已实现 | `BadcaseService.filter_badcases`；`GET /badcases` 支持状态、问题类型、原因、Skill、关键词、得分区间；无分页参数时保持 legacy 数组响应，带 `page/page_size` 时返回 `{ items, pagination }`，支持筛选后分页 |
| FR-RP-04 | 单次任务报告 | 已实现基础 | API `/tasks/{task_id}/report` 包装任务摘要、版本快照、Step 分布、Judge 分数分布、RunReport、Badcase、导出链接和 Task Diagnostics；React 报告中心围绕 Task 展示“评测结论”第一屏、摘要、版本、指标、根因诊断、Step 分布和 Badcase，并可从诊断一键生成 Repair Task；报告中心任务选择器使用 `GET /tasks?page=1&page_size=20` 加载最近任务，并支持 `GET /tasks?q=...&page=1&page_size=20` 远程搜索历史任务；深链 `task_id` 不在最近列表时使用 `GET /tasks/{task_id}` 精准加载单任务，目标任务加载完成前不会请求最近列表第一条任务报告，避免为了打开一份报告扫描全量任务历史或短暂展示错误报告；Playwright E2E 覆盖任务报告查看、导出和 Badcase 加入 Golden |
| FR-ME-01 | Judge Profile 管理 | 已实现基础 | `JudgeProfileService.create_profile/get_profile`，`POST /judge-profiles`；创建 Profile 要求 `judge:audit`，Viewer 会被结构化拒绝，默认 `Reviewer/api` 兼容旧前端，成功/拒绝审计记录 actor/role |
| FR-ME-02 | Golden Dataset 裁判评测 | 已实现 | `audit_judge_profile` 输出 Accuracy/Precision/Recall/F1/Kappa/混淆矩阵；`POST /judge-audits` 一次性审计要求 `judge:audit`，Viewer 会被结构化拒绝，成功/拒绝审计记录 actor/role |
| FR-ME-03 | 审计结果入库 | 已实现 | `JudgeProfileService.audit_and_store`；`POST /judge-profiles/{profile_id}/audits` 要求 `judge:audit`，Viewer 会被结构化拒绝，成功/拒绝审计记录 actor/role |
| FR-HL-01 | Badcase 列表和详情 | 已实现基础 | Report badcases、BadcaseService |
| FR-HL-02 | 人工标签/说明/责任归类 | 已实现 | `correct_badcase`；报告中心支持加入 Golden、忽略、重开、加入 Annotation Queue |
| FR-HL-03 | 加入错题本/Golden 候选 | 已实现基础 | `golden_candidate` 与 `accepted_to_golden` |
| FR-HL-04 | 处理状态流转 | 已实现 | pending_review/corrected/accepted_to_golden/ignored/reopened；React Badcase 表格提供单条状态动作 |
| FR-AU-01 | 敏感配置脱敏 | 已实现 | `redact_secrets`，保留 tokens 成本指标 |
| FR-AU-02 | 运行快照 | 已实现 | Run snapshot 记录 Workflow、Skill、Dataset、Runtime |
| FR-AU-03 | 可信 Skill 来源限制 | 已实现基础 | 内置/可信 manifest 仍可扫描；上传 Agent Skill 包默认待审批，合约测试通过并审批后才能被新 Workflow 引用；脚本型上传包通过短生命周期子进程执行，不 import 到 FastAPI 主进程；说明型上传包通过模型网关执行；下一阶段仍需容器沙箱、依赖隔离、网络白名单和资源配额 |

## P1/P2 功能覆盖

| 编号 | 需求 | 当前状态 | 证据 |
|---|---|---|---|
| FR-SK-06 | Skill 启用/禁用/审批/废弃 | 已实现基础 | `SkillRegistry.disable/approve/deprecate`，`POST /skills/{skill_id}/approve|disable|deprecate`，React Skill 市场展示待审批、合约状态、审批人和审批时间；治理页新增审批抽屉展示 Manifest、Schema、测试日志，未通过合约测试的插件不能在前端直接启用；内置 Skill manifest 在实例化时深拷贝，禁用/审批状态不会跨 `SkillRegistry` 实例串扰；上传、审批、禁用、废弃和回滚成功审计会记录请求 actor/role，便于追责关键 Skill 生命周期变更 |
| FR-SK-07 | Skill 安全策略声明 | 已实现基础 | `SkillManifest.permissions` |
| FR-WF-05 | 模板化 Workflow | 已实现基础 | `WorkflowTemplateService`，React Workflow 页面提供示例图 |
| FR-WF-06 | DAG/条件分支/并行节点 | 已实现基础 | `DAGWorkflow.execution_levels`、`DAGWorkflowExecutor` 支持条件 `exists/not exists` 和按层并行执行；`WorkflowGraphService` 支持画布 DAG 校验、点对多、多对一 Join/Aggregator 规则；发布前阻断测试覆盖未知 Skill、未审批 Skill、多对一缺 Join/Aggregator、Branch 缺条件表达式和必填输入映射缺失 |
| FR-DS-07 | DBQuery/API/线上抽样 Source Skill | 已实现基础 | `DBQuerySkill` 支持 sqlite SQL，`APIPullSkill` 支持 file/http JSON rows，`OnlineSampleSkill` 支持抽样 |
| FR-DS-08 | 数据导出 | 已实现基础 | `DatasetService.export_rows` |
| FR-EX-06 | 取消、暂停、恢复 | 已实现基础 | `cancel_run/pause_run/resume_run`，`POST /runs/{run_id}/execute|pause|resume|cancel|retry-failed`，React 执行中心已接入；Task 层已增加 completed/running/canceled 状态机保护；`POST /tasks/{task_id}/pause|resume|cancel|retry-failed` 统一要求 `run:control`，成功和拒绝都会进入审计 |
| FR-EX-07 | sample_repeat_times | 已实现基础 | Run Item repeat_index 与测试 |
| FR-EX-08 | Step 级 Evaluation Cache | 已实现基础 | cache_key 与 cache_hit 记录 |
| FR-RP-05 | 跨 Run/Task 趋势图 | 已实现基础 | `compare_reports` 输出趋势差值；`GET /score-analytics` 按 Task 聚合通过率、错误率、Badcase、P95 耗时、估算成本和退化任务，并支持 `dataset_id/workflow_id/status/page/page_size`；React 报告中心按当前任务的 Dataset + Workflow 作用域请求趋势，趋势表使用服务端分页；Experiment 页面继续提供 A/B 对比入口 |
| FR-RP-06 | 多次运行聚合视图 | 已实现基础 | `aggregate_repeat_items` 输出多数投票、通过概率、方差、不稳定样本 |
| FR-RP-07 | 报告导出 | 已实现基础 | `export_report_csv`、`export_report_html`，`GET /runs/{run_id}/report/export?file_format=json|csv|html` 保留 Run 级导出，legacy Run HTML 导出会转义 `run_id` 和完整 JSON payload，避免用户样本、Badcase payload 或模型输出里的脚本片段进入 HTML；新增 `GET /tasks/{task_id}/report/export?file_format=json|csv|html` 作为产品主入口，导出 Task、Run Report、Badcase 和创建前 Preflight 证据；CSV 导出包含任务指标、Preflight 检查、质量决策、分层分析和 Badcase 明细，并会对 `= + - @` 公式前缀加单引号，避免用户下载后被 Excel/WPS 当公式执行；`GET /tasks/{task_id}/results/export?file_format=csv` 同样保护原始 `row.*`、`context.*`、`metrics.*` 单元格；HTML 导出按章节展示任务摘要、质量决策、Preflight 检查、分层分析、Badcase 明细和 Report，并转义任务名和 JSON 内容；导出成功会写入 `task.report.export` 审计事件；报告中心前端会将导出内容转成 Blob 并触发浏览器下载，且 HTML/CSV/JSON 三个按钮分别调用对应格式；报告中心展示当前 Task 的导出历史，按审计事件追踪格式、Run、Preflight ID、操作者和时间；Task Report Export 已接入 `report:export` 权限，Viewer 只读角色会被 `REPORT_EXPORT_FORBIDDEN` 阻断；新增导出审批流，Viewer 可创建审批请求，Admin 审批后可携带 `approval_request_id` 导出同一 Task 与格式；审批请求支持默认 24 小时过期、Admin 拒绝、申请人撤销和过期阻断 |
| FR-ME-04 | 裁判偏差分析 | 已实现基础 | `JudgeProfileService.bias_analysis`；新增 `GET /judge-audits/trends` 按 Judge Profile 聚合 Accuracy/Kappa 趋势和低一致性告警；React Judge 审计页展示“Judge 偏差趋势” |
| FR-ME-05 | 人工纠错反哺候选池 | 已实现基础 | `PromptCandidateService` 可从 Badcase 创建 Prompt 优化候选并评审 |
| FR-ME-06 | 多裁判交叉验证 | 已实现基础页面 | `JudgeProfileService.cross_validate`，`POST /judge-cross-validation`，React Judge 审计页提供“多 Judge 一致性”弹窗并展示两两一致率 |
| FR-HL-05 | Badcase 聚类 | 已实现 | `cluster_badcases(method="rule")` 与 `cluster_badcases(method="embedding")` |
| FR-HL-06 | 批量处理与导出 | 已实现基础 | `bulk_correct`、`export_badcases`，`POST /badcases/bulk-correct`，`GET /badcases/export`；报告中心支持勾选多条后批量加入 Golden |
| FR-AU-04 | 角色权限 | 已实现基础 | `AccessControl`；报告导出区分 `report:read` 与 `report:export`，Evaluator/Reviewer/Admin 可导出，Viewer 只能查看；导出审批使用 `report:export:approve`，当前由 Admin 通过 `*` 权限审批，Reviewer 审批会返回 `REPORT_EXPORT_APPROVAL_FORBIDDEN`；撤销导出审批要求原申请角色匹配，否则返回 `REPORT_EXPORT_REVOKE_FORBIDDEN`；Dataset 上传、Source 物化、字段类型修正和修复版生成要求 `dataset:create`，Viewer 会被结构化拒绝；Judge Profile 创建、一次性 Judge 审计和 Profile 审计要求 `judge:audit`，Reviewer 可操作，Viewer 会被结构化拒绝；Task Preflight 和执行参数模板创建要求 `run:create`，Evaluator 可操作，Viewer 会被结构化拒绝；Red Team 扫描要求 `redteam:scan`，Experiment 快照创建要求 `experiment:create`，CI Gate 创建和评估要求 `ci_gate:manage`，Evaluator 可操作，Viewer 会被结构化拒绝；Candidate Assets 单条审核、批量审核、批量指派、批量归档、逾期升级、生成 Workflow 草稿、候选复跑、创建晋升审批和晋升审批结论要求 `candidate:govern`，Evaluator/Reviewer 可操作，Viewer 会被结构化拒绝；Annotation Queue 生成、自动分派、指派、单条审核和批量审核要求 `annotation:review`，Evaluator/Reviewer 可操作，Viewer 会被结构化拒绝；Repair Task 生成、领取、指派、完成、重开和动作入口要求 `badcase:correct`，Evaluator/Reviewer 可操作，Viewer 会被结构化拒绝；Workflow 草稿创建/更新/删除、复制、归档和发布写操作要求 `workflow:publish`，Viewer 会被结构化拒绝；Task 新建 Attempt 要求 `run:create`，暂停、恢复、取消、重试失败项要求 `run:control`，Evaluator 默认具备任务控制能力，Viewer 会被 `FORBIDDEN` 结构化拒绝；Agent Skill 本机导入要求 `skill:register`，Viewer 会被结构化拒绝，默认 Skill Developer/api 保持旧脚本兼容；模型网关配置保存、连接别名增删改和连接测试统一要求 `model:configure`，Viewer 会被结构化拒绝，默认 Admin/api 保持旧前端兼容 |
| FR-AU-05 | 操作审计 | 已实现基础 | `AuditService`，`GET /audit-events` 支持按 actor/action/target 过滤，回归测试覆盖过滤不再返回 500，并支持按 Task 追踪报告导出事件；Task Preflight 和执行参数模板创建的成功/拒绝审计会记录请求 actor/role，避免任务创建前证据链只能追溯到泛化 `api`；Judge Profile 创建、一次性 Judge 审计和 Profile 审计的成功/拒绝审计会记录请求 actor/role，避免 Judge 治理动作只能追溯到泛化 `api`；Dataset Source 物化和字段类型修正的成功/拒绝审计会记录请求 actor/role，Source 物化 forbidden 审计使用 `dataset.source_materialize`，字段修正 forbidden 审计使用 `dataset.field_type.correct`，避免数据来源和字段治理动作只能追溯到泛化 `api`；Red Team 扫描、Experiment 快照创建、CI Gate 创建和 CI Gate 评估的成功/拒绝审计会记录请求 actor/role，避免安全扫描、实验快照和发布门禁资产只能追溯到泛化 `api`；Candidate Assets 单条审核、批量审核、批量指派、批量归档、逾期升级、生成 Workflow 草稿、候选复跑、创建晋升审批和晋升审批结论的成功/拒绝审计会记录请求 actor/role，复跑生成 Task/Experiment 和晋升发布资产不再固定追溯到 `api`；Baseline apply、Baseline 变更提醒 ack 和 Baseline rollback 的成功审计会记录请求 actor/role，并在 `detail.role` 中保留角色上下文，避免生产 baseline 切换链路只能看到操作者姓名而缺少角色；Annotation Queue 生成、自动分派、指派、单条审核和批量审核的成功/拒绝审计会记录请求 actor/role，候选资产 reviewer 使用真实 actor，避免人工审核和 Golden/Assertion 候选沉淀只能追溯到泛化 `api`；Repair Task 生成、领取、指派、完成、重开和动作入口的成功/拒绝审计会记录请求 actor/role，且修复任务派生的新 Attempt 与版本差异 Workflow 草稿也记录请求 actor/role，避免修复链路、人工审核、CI Gate 复测、候选资产沉淀和复跑派生资产只能追溯到泛化 `api`；Workflow 草稿创建/更新/删除、复制、归档和发布写操作的成功/拒绝审计会记录请求 actor/role，避免可执行资产变更只能追溯到泛化 `api`；Task 创建、执行、新建 Attempt、暂停、恢复、取消、重试失败项的成功/拒绝审计会记录请求 actor/role，避免执行链路只能追溯到泛化 `api`；Agent Skill 本机导入的成功/拒绝审计会记录请求 actor/role，避免本机 Skill 资产导入只能追溯到泛化 `api`；模型网关配置、连接别名增删改和连接测试的成功/拒绝审计会记录请求 actor/role，且不记录明文 API Key；Task 原始结果导出 `task.results.export` 成功事件会记录请求 `actor` 与 `role`，避免样本级敏感数据下载只能追溯到泛化 `api`；Skill 生命周期成功审计和插件包生命周期元数据都会记录请求 actor/role，避免上传、审批、禁用、废弃和回滚在审计日志与 Skill 市场包记录之间出现归因割裂 |

## 市场对标增强覆盖

| 对标能力 | 当前状态 | 证据 |
|---|---|---|
| Task 一等模型 | 已实现基础 | `GET/POST /tasks`、`GET /tasks/{task_id}`、`POST /tasks/preflight`、`GET/POST /task-execution-templates`、`POST /tasks/{task_id}/attempts`、`POST /tasks/{task_id}/execute|pause|resume|cancel|retry-failed`、`GET /tasks/{task_id}/report`、`GET /tasks/{task_id}/trace-tree`；`GET /tasks` 无分页参数时保持 legacy 数组响应，带 `page/page_size` 时返回 `{ items, pagination }`，并支持 `status/dataset_id/workflow_id/q` 过滤后分页；底层 `GET /runs` 也提供兼容式轻量分页，便于 Dashboard、Experiment、CI Gate 和旧脚本按 Attempt 摘要选择 Run，而不一次性拉取所有 Run Item 明细；`GET /dashboard/summary` 使用 Run 摘要计算 run_count/latest_run，只在最近 completed Run 报告需要时读取单个完整 Run；Task 创建保存 `execution_template_id`、`evaluation_goal`、`quality_gate`、`preflight_result` 和 `execution_config`，包含并发、repeat、重试、成本预算和 `allow_blocked_preflight` 风险接受标记；Run Attempts 保留旧 Run 报告快照；Task 控制动作、新建 Attempt、Task Preflight 和执行参数模板创建均已接入权限与审计归因；前端执行中心默认展示任务列表并使用服务端分页、任务搜索、状态筛选和带 Preflight 门禁/执行参数模板的独立创建向导；首页、Experiment 和 CI Gate 不再请求 legacy `/runs` 完整列表；创建向导仅在打开时挂载 Form，关闭状态不再产生 `useForm` 未连接 warning；报告中心深链可按 `task_id` 单任务加载；Playwright E2E 覆盖创建和执行任务 |
| Experiment 快照与 baseline 对比 | 已实现基础页面 | `POST /experiments/from-run`，`GET /experiments?dataset_id=&workflow_id=`，保存 Workflow/Dataset/Skill/Prompt/Runtime 快照与 baseline diff；创建快照要求 `experiment:create`，Viewer 会被结构化拒绝，成功/拒绝审计记录 actor/role；React `/experiments` 页面展示实验快照列表、Dataset/Workflow 过滤、baseline 选择、A/B 对比面板、通过率、Badcase、P95 耗时、成本和失败分布变化，并通过 `GET /runs?page=1&page_size=100` 读取轻量 Run 摘要来生成实验快照 |
| Prompt / Skill 版本注册 | 已实现基础 | Run snapshot 与 Experiment snapshot 记录 `skill_versions`、`prompt_skill_versions`、模型参数 |
| Assertion DSL | 已实现最小 API | `POST /assertions/evaluate` 支持 contains、regex、json_schema、similarity、latency、cost、safety 的基础断言 |
| CI Gate | 已实现基础页面 | `GET/POST /ci-gates` 支持质量门禁配置保存和列表；`POST /ci-gates/evaluate` 支持按配置和指标阈值 blocking 发布，也支持直接对 Task/Run 抽取指标评估，并保存 `gateeval-*` 历史；创建配置和执行评估要求 `ci_gate:manage`，Viewer 会被结构化拒绝，成功/拒绝审计记录 actor/role；`GET /ci-gates/evaluations` 支持按 config、task、run 过滤，无分页参数时保持 legacy 数组响应，带 `page/page_size` 时返回 `{ items, pagination, summary }`，summary 基于筛选后的全量历史计算；React `/ci-gates` 页面支持创建门禁配置、选择 Task/Run 执行评估，Run 选择器通过 `GET /runs?page=1&page_size=100` 读取轻量摘要，并展示阻断原因、实际值、阈值、历史趋势和服务端分页评估历史；Playwright 覆盖真实创建和阻断评估 |
| Annotation Queue | 已实现基础页面 | `POST /annotation-queue/seed-from-run`、`GET /annotation-queue`、分派、review、`POST /annotation-queue/bulk-review`、`GET /annotation-candidates`；队列记录回填来源 Task，支持状态/负责人/来源任务筛选；`GET /annotation-queue?page=&page_size=` 返回服务端分页结果且无分页参数时保持 legacy 数组响应；生成、自动分派、指派、单条审核和批量审核要求 `annotation:review`，Evaluator/Reviewer 可操作，Viewer 会被结构化拒绝，成功/拒绝审计记录 actor/role，候选资产 reviewer 使用真实 actor；React `/annotation-queue` 页面支持领取、分派、审核、批量审核、回流 Golden Dataset、候选资产摘要和服务端分页；Playwright 覆盖真实领取、审核、批量审核和回流 |
| Prompt/Skill 候选资产服务端分页 | 已实现兼容分页 | `GET /prompt-skill-candidates` 无分页参数时保持 legacy 数组响应，带 `page/page_size` 时返回 `{ items, pagination }`，并在状态、来源任务、baseline 实验筛选后再分页；React `/candidate-assets` 主表改为服务端分页，点击翻页会重新请求后端，状态筛选变化回到第 1 页，批量审批、批量指派和批量归档基于当前页候选资产执行 |
| Prompt/Skill 候选资产治理 | 已实现基础页面 | `GET /prompt-skill-candidates` 支持来源任务、状态、baseline 实验筛选，默认隐藏 archived 候选，`status=archived` 可查看归档记录；`GET /prompt-skill-candidates/workload` 按负责人聚合候选总量、待处理、逾期和已升级数量；`GET /prompt-skill-candidates/retest-plan` 按候选草稿状态、发布状态、复跑结果、逾期、升级和晋升建议计算优先级、下一步动作、原因和入口；候选资产单条审核、批量审核、批量指派、批量归档、逾期升级、生成 Workflow 草稿、候选复跑、创建晋升审批和晋升审批结论统一要求 `candidate:govern`，Viewer 会被结构化拒绝，Evaluator/Reviewer 成功/拒绝审计记录 actor/role；`POST /prompt-skill-candidates/bulk-retest` 会基于复跑计划只执行 `retest_candidate` 候选，并把待发布、待建草稿、已复跑或异常候选作为 skipped 返回，同时写入候选 `action_history`、Task、Experiment 和审计事件；`POST /prompt-skill-candidates/bulk-assign` 支持批量设置负责人、截止时间、操作历史和 `max_open_per_owner` 负责人开放候选容量限制，超容量候选进入 skipped 明细并返回 `capacity` 摘要；`POST /prompt-skill-candidates/bulk-archive` 支持按状态和 `stale_before` 归档 rejected/promoted/retested/promotion_rejected 终态候选，开放候选和未到清理水位线候选会进入 skipped 明细；`POST /prompt-skill-candidates/escalate-overdue` 支持把超出 SLA 的开放候选升级；`POST /prompt-skill-candidates/bulk-review` 支持批量 approved/rejected 审批当前列表；`POST /prompt-skill-candidates/{candidate_id}/review` 支持 approved/rejected 审批并保留 review_history；`POST /prompt-skill-candidates/{candidate_id}/workflow-draft` 要求候选审批通过后才能创建 Workflow 草稿，未审批返回 `PROMPT_SKILL_CANDIDATE_NOT_APPROVED`；`POST /prompt-skill-candidates/{candidate_id}/retest` 要求候选草稿已发布，随后复用来源 Task 的 Dataset Version 和执行配置创建候选 Task、自动执行、生成候选 Experiment，并返回 baseline/current/candidate 三方指标、current_to_candidate、baseline_to_candidate delta 和 `promotion_recommendation` 晋升建议；晋升建议会解释通过率门槛、Badcase 门槛、相对当前版本改善、baseline 退化检查和下一步动作；`POST /prompt-skill-candidates/{candidate_id}/promotion-review`、`GET /workflow-promotion-reviews`、`POST /workflow-promotion-reviews/{review_id}/approve|reject` 支持 Workflow 晋升审批创建、查询和结论回写；审批通过后返回 `release_artifacts`，并持久化 `experiment_baseline_suggestions`、`workflow_release_records` 和来源为 `workflow_promotion_review` 的 CI Gate 发布评估历史；`GET /experiment-baseline-suggestions` 与 `GET /workflow-release-records` 支持按 candidate/review/status 查询；`GET /experiment-baselines`、`GET /experiment-baseline-suggestions/{suggestion_id}/impact` 和 `POST /experiment-baseline-suggestions/{suggestion_id}/apply|rollback` 支持按 Dataset + Workflow 管理当前 baseline、分析影响任务和指标 delta、应用/回滚 history，并在回滚前生成来源为 `experiment_baseline_rollback` 的 CI Gate evaluation；React `/candidate-assets` 页面展示候选列表、负责人工作量、可编辑批量指派负责人、可编辑负责人容量上限、复跑优先级、批量复跑可执行候选、未指派/逾期/已升级摘要、版本差异、状态筛选、批量指派、容量跳过反馈、归档终态候选、归档/跳过反馈、批量审批、逾期升级、审批通过、拒绝、生成草稿、复跑对比、三方指标卡、晋升建议、晋升审批、通过晋升、Baseline 替换建议、查看影响、应用 baseline、回滚 baseline、当前 baseline、回滚门禁、CI Gate 发布记录和候选任务报告入口 |
| Baseline 变更提醒 | 已实现基础页面 | `GET /baseline-change-notifications` 支持按 suggestion、baseline、workflow、status 查询 baseline 变更提醒；`POST /baseline-change-notifications/{notification_id}/ack` 支持确认已读；baseline apply/rollback 响应返回 `notifications`，每条提醒包含接收人、影响任务、报告数、指标 delta 和回滚门禁状态；React `/candidate-assets` 页面展示“Baseline 变更提醒”卡片，可确认已读并刷新提醒列表；后端目标测试覆盖 apply 提醒、列表查询、ack 和 rollback 提醒，前端目标测试覆盖应用 baseline 后展示提醒和确认已读 |
| Trace Tree | 已实现独立页面 | `GET /runs/{run_id}/trace-tree` 与 `GET /tasks/{task_id}/trace-tree` 展示 Run Item -> Skill Step 输入、输出、耗时、错误、缓存命中；React `/tasks/:taskId/trace-tree` 独立页面展示 Item 调用树 |
| Task 参数治理 | 已实现基础 | `GET /tasks/{task_id}/parameter-governance` 和 Task Report `parameter_governance` 展示 Skill/Prompt 版本、模型参数、任务覆盖和脱敏 Secret 策略 |
| Task 执行参数模板 | 已实现基础 | `GET /task-execution-templates` 返回上线门禁稳健模板、Prompt 实验快速模板、稳定性重复采样模板和自定义模板；`POST /task-execution-templates` 可保存自定义评测目的、质量门槛和执行参数；创建模板要求 `run:create`，Viewer 会被结构化拒绝，默认 `Evaluator/api` 兼容旧前端，成功/拒绝审计记录 actor/role；创建 Task 可携带 `execution_template_id` 并进入 Task/Run 快照；React 创建任务向导支持选择模板并填充质量门槛、并发、repeat、重试和成本预算，且关闭态不提前创建 Form 实例 |
| Task Preflight | 已实现基础 | `POST /tasks/preflight` 在创建任务前检查数据集非空、Workflow 字段映射、Workflow Step schema 映射、Skill 参数配置、Golden 覆盖、Skill 审批状态、质量门槛和成本预算，并持久化返回 `preflight_id`；运行 Preflight 要求 `run:create`，Viewer 会被结构化拒绝，默认 `Evaluator/api` 兼容旧前端，成功/拒绝审计记录 actor/role；`workflow_schema_mapping` 会阻断历史坏 Workflow 的 Skill 必填映射缺失、空输入路径和空输出写入路径；`skill_config` 会结合 Dataset 预览样本、Workflow config 和任务级 `skill_overrides` 校验最终参数，阻断必填参数缺失、override 类型错误、表达式路径缺失和 Secret 引用声明错误，并返回出错 `row_index`；Workflow Console 和任务创建错误流会围绕同一批 `CONFIG_*` 错误码给出中文修复建议，减少用户在发布期和创建期之间来回猜参数来源；`GET /task-preflights/{preflight_id}` 可查询用户创建任务前实际看过的预检证据；Preflight 响应返回 `execution_template_id`、`evaluation_goal`、`quality_gate`、`sample_repeat_times`、`cost_budget` 和 `skill_overrides` 关键参数签名；`POST /tasks` 永远基于当前请求重算服务端 Preflight 作为事实源，客户端提交的 `preflight_result` 只用于签名过期判断，不能伪造 passed 绕过字段缺失、Workflow schema 映射缺失、Skill 参数错误、Skill 未审批、预算等阻断项；`POST /tasks` 可引用 `preflight_id`，并把它写入 Task `preflight_result` 与 `execution_config`；显式 `preflight_id` 与客户端 `preflight_result.preflight_id` 冲突时返回 `TASK_PREFLIGHT_STALE`；`GET /tasks/{task_id}/report` 返回 `preflight_evidence`，报告中心摘要会展示创建前状态、ID 和摘要；`GET /tasks/{task_id}/report/export` 的 JSON/CSV/HTML 导出都会包含创建前 Preflight 证据；`POST /tasks` 会阻断 `status=blocked` 的服务端 Preflight 并返回 `TASK_PREFLIGHT_BLOCKED`，也会拒绝与当前请求不匹配的旧 `preflight_result` 并返回 `TASK_PREFLIGHT_STALE` 与 mismatch 字段；确需创建坏数据诊断任务时必须显式传 `allow_blocked_preflight=true`，该标记会进入 Task/Run 执行快照和审计；React 任务创建向导要求先运行与当前 Dataset/Workflow/执行模板/质量门槛/repeat/成本预算/Skill 覆盖匹配的 Preflight，关键参数变更后必须重跑；向导已提供任务级 Skill 参数覆盖表格，选择 Step 后可从 Skill `config_schema` 选择参数名并自动推断字符串、数字、布尔、JSON 类型，表达式路径可从当前 Dataset Version 字段候选中选择，仍支持 Secret 引用，并把 `skill_overrides` 透传到 `/tasks/preflight` 与 `/tasks`；选择执行模板后创建任务会提交 `execution_template_id` 与 `preflight_id`，阻断时默认禁用创建并要求勾选风险确认；任务详情参数页会展示创建前 Preflight ID、状态、摘要和检查项，方便用户复核创建前证据 |
| 质量决策中心 | 已实现基础 | Task Report `quality_decision` 把通过率、错误率、Badcase 和低分层转为 passed/warning/blocked 决策、风险摘要和下一步动作；React 报告中心新增“评测结论”第一屏，先回答“能否发布 / 为什么 / 影响多大 / 下一步” |
| Task Diagnostics 根因诊断 | 已实现基础 | `GET /tasks/{task_id}/diagnostics` 与 Task Report `diagnostics` 汇总运行时错误、低通过率分层、字段缺失/重复、Step 健康度和参数风险；React 报告中心展示主要根因、证据数、根因表、数据质量和修复动作，且 next_actions 已按钮化，可直接进入 Trace Flow、数据血缘、Judge 审计，或调用 Annotation Queue、CI Gate、失败项重试 |
| Repair Task 工作台分页 | 已实现兼容分页 | `GET /repair-tasks` 无分页参数时保持 legacy 数组响应，带 `page/page_size` 时返回 `{ items, pagination }`，并支持 `source_task_id` 与 `status` 过滤后分页；React `/repair-tasks` 工作台主表使用服务端分页，状态/来源任务筛选变化回到第 1 页，领取、完成、重开、指派和后续修复动作保持兼容 |
| Repair Task 修复闭环 | 已实现基础 | `POST /tasks/{task_id}/repair-tasks/from-diagnostics` 可把诊断 root cause 沉淀为可追踪修复任务；`GET /repair-tasks?source_task_id=` 支持按来源任务查询；`GET /repair-tasks/{repair_task_id}/tree` 返回父任务、子任务、完成率、阻塞子任务、逾期子任务和下一步动作；`POST /repair-tasks/{repair_task_id}/assign` 支持设置负责人、截止时间、`assigned_at` 和 `overdue`；`POST /repair-tasks/{repair_task_id}/start|resolve|reopen` 支持领取、完成、重开并记录审计，完成任务会清除逾期标记；`POST /repair-tasks/{repair_task_id}/actions` 支持从修复任务发起 Annotation Queue、CI Gate 复测、`retest_and_compare` 复跑对比、`generate_remediation_plan` 上下文修复建议、`create_followup_repair_tasks` 二级修复任务、`fix_dataset_fields` 字段修复计划、`plan_workflow_parameter_changes` 参数 diff/回滚计划、`compare_prompt_skill_versions` Prompt/Skill 版本对比计划、`create_prompt_skill_candidate` 候选资产沉淀和 `create_workflow_draft_from_version_diff` Workflow 草稿创建，并写入 `action_history` 与 `last_action_result`；上述生成、状态流转、指派和动作入口统一要求 `badcase:correct`，Viewer 会被结构化拒绝，成功/拒绝审计记录 actor/role；字段修复计划会返回 Dataset Version、缺失 Workflow 必需字段、字段覆盖率、重复样本数、字段级建议和数据集入口；参数 diff/回滚计划会返回 Workflow 默认值、当前执行值、task_override 移除项、runtime_expression/secret_ref 复核项和参数治理入口；Prompt/Skill 版本对比计划会基于同数据集 Experiment baseline 返回 `skill_ref`、`skill_version`、`prompt_version`、`model`、`model_params` 差异、baseline 候选和候选修复动作；候选资产动作会把 baseline、当前版本和 diff 写入 `prompt_skill_candidates`；Workflow 草稿动作会把 baseline Prompt/模型/Skill 配置回填到新草稿图中但不直接发布，并记录请求 actor/role；复跑动作会创建新 Attempt、自动执行、对比前后 RunReport、记录请求 actor/role，并返回 improved / mixed / unchanged / regressed；修复建议会结合复跑状态、诊断根因、弱分层、数据质量、参数治理风险和版本变化风险，输出 annotation、dataset、workflow_parameters、prompt_skill_versions、retest 等下一步动作；二级任务会保留父任务、推荐动作、目标入口、负责人、截止时间和逾期状态，并重复点击复用已有任务；React 报告中心可一键生成修复任务，`/repair-tasks` 工作台支持筛选、查看证据、指派负责人、查看逾期、领取、完成、重开、发起人工审核、CI Gate 复测、复跑对比、生成修复建议、拆分子任务、字段修复计划、参数 diff/回滚计划、Prompt/Skill 版本对比、沉淀候选、生成 Workflow 草稿、沉淀后继续生成草稿、查看修复树进度、查看动作历史、最近建议和推荐动作入口，并跳回来源报告、Trace、参数治理和数据集页 |
| 成本预算状态 | 已实现基础 | Task Report 新增 `budget_status`，基于报告 cost 或 token 估算成本，输出 ok/warning/exceeded/not_set、预算、已用、剩余和修复建议；React 报告中心展示“成本预算” |
| 红队安全扫描 | 已实现基础 | `POST /red-team/scans` 支持按 Task/Run 做规则化扫描，识别 prompt injection、PII、unsafe content、secret exposure，保存扫描记录并生成下一步建议；React 报告中心提供“运行红队扫描”入口 |
| 产品化入口 | 已实现基础 | React 首页读取真实 Dashboard；Workflow 先进入市场，执行与报告围绕 Task 组织 |

## 生产化边界说明

- 本地默认运行仍使用 JSON 文件仓储和单进程 Runner，便于面试演示和无外部依赖验证；JSON Store 已增加锁文件、原子写入和列表读取文档锁，降低本地多线程/多进程测试时的文件损坏与 Windows `os.replace` 读写冲突风险，但不提供事务、索引、权限隔离、分布式一致性或高可用能力。
- 已新增 SQLite 轻量元数据仓储，可通过 `create_app(..., storage_backend="sqlite")` 或 `AEGISQA_STORAGE_BACKEND=sqlite` 启用；Task、Run、Workflow、Judge、审计等 JSON 文档进入 SQLite，Dataset rows、上传文件和 Skill 插件包仍保留本地文件路径，适合单机长期试用和小团队验证。SQLite JSONL 覆盖写和追加写已使用 `BEGIN IMMEDIATE` 串行化同一 stream 的写入，避免并发审计事件重复分配 `row_index`；它仍不是分布式高可用数据库。
- MySQL 生产适配契约已提供 `MySQLStore`、`infra/mysql/schema.sql` 和 fake MySQL 回归测试；MySQL JSONL stream 覆盖写与追加写已通过 MySQL named lock 串行化同一 stream 的 `row_index` 分配，避免并发审计事件撞主键。本轮没有启动真实 MySQL/Docker，外部服务连通性、隔离级别和性能仍需部署环境实测。
- 产品主线以 Task 为中心；Run 是底层执行 Attempt，用于承载 Run Item、Step Trace、队列消息和报告快照。前端执行中心、报告中心、Annotation 和 CI Gate 都应优先通过 Task 入口组织用户流程。
- Skill 插件包执行默认走受控子进程，不在主 FastAPI 进程中直接 import 用户代码；当前已具备审批门禁、60 秒默认超时、stdout 输出上限、日志截断和本地路径脱敏，生产环境仍需补进程级 CPU/内存限额、依赖隔离和签名校验。
- 生产适配资产已提供：`docker-compose.yml`、`infra/mysql/schema.sql`、`infra/celery/README.md`、`aegisqa/workers/celery_app.py`、`aegisqa/infrastructure/manifest.py`。
- MySQL/Redis/Celery 生产服务需要在目标环境中安装依赖并启动容器后接入真实 Repository/Worker；当前测试验证了 schema、消息契约和 Worker 入口，而不是启动外部服务。
- DBQuery 当前真实执行 sqlite，MySQL 连接池是生产 Repository/Skill Adapter 的自然扩展点；API Pull 支持 file/http JSON rows，企业认证和分页策略可在 config 中继续扩展。
- DAG 执行器已支持条件和按层并行；React Flow 可视化画布已提供产品化入口。复杂表达式语言、企业级调度和真实分布式 Worker 仍属于后续生产增强。
