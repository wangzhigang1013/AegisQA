[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aegisqa"
version = "0.1.0"
description = "AegisQA: AI 工程化治理与全链路评测平台 MVP"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "pydantic>=2",
  "pymysql>=1.1",
  "PyYAML>=6",
  "redis>=5",
  "streamlit>=1.30",
  "uvicorn[standard]>=0.29",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
